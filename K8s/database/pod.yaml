apiVersion: v1
kind: Pod
metadata:
  name: mysql-db-pod
  labels:
    app: mysql-db
spec:
  containers:
  - name: mysql
    image: mysql:latest
    env:
    - name: DB_PASSWORD
      value: "passwors"
    - name: DATABASE
      value: "employees"
    ports:
    - containerPort: 3306

  
